# PocketMind 局域网 P2P 同步模块

## 概述

本模块实现了基于局域网的 P2P（点对点）数据同步功能，允许多台设备在同一局域网内实时同步笔记和分类数据。

### 核心特性

- 🔄 **双向同步**：支持设备间的双向数据同步
- ⚡ **实时通知**：通过 WebSocket 实现实时变更通知
- 🛡️ **冲突解决**：采用 Last-Write-Wins (LWW) 策略
- 🔍 **设备发现**：通过 WebSocket 扫描局域网发现其他设备
- 📱 **跨平台**：支持 Android、iOS、Windows、macOS
- 🚀 **纯 WebSocket**：单一端口，简洁高效

---

## 架构设计

```
┌─────────────────────────────────────────────────────────────────┐
│                         UI Layer                                 │
│                    (sync_settings_page.dart)                     │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SyncService (Riverpod)                      │
│  - 状态管理 (SyncServiceState)                                   │
│  - 服务启停控制                                                   │
│  - 设备发现协调                                                   │
│  - WebSocket 连接管理                                            │
└─────────────────────────────────────────────────────────────────┘
                                │
                ┌───────────────┴───────────────┐
                ▼                               ▼
┌───────────────────────────────┐ ┌──────────────────────────────┐
│       SyncWebSocketServer      │ │       SyncManager            │
│     (服务端 + 实时通知)         │ │      (核心调度器)             │
│        Port: 54322             │ │                              │
└───────────────────────────────┘ └──────────────────────────────┘
                │                               │
                └───────────────┬───────────────┘
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     SyncWebSocketClient                          │
│                    WebSocket 客户端连接                           │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                   SyncDataMapper (JSON 转换)                     │
│  - noteToJson / noteFromJson                                     │
│  - categoryToJson / categoryFromJson                             │
│  - combineChanges                                                │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Isar Database                            │
│  - Note (uuid, updatedAt, isDeleted)                             │
│  - Category (uuid, updatedAt, isDeleted)                         │
│  - SyncLog (同步日志)                                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 目录结构

```
lib/sync/
├── README.md                    # 本文档
├── sync.dart                    # 模块导出文件
├── sync_service.dart            # 同步服务 (Riverpod StateNotifier)
├── sync_manager.dart            # 核心同步管理器
├── realtime/
│   ├── sync_websocket_server.dart  # WebSocket 服务端
│   └── sync_websocket_client.dart  # WebSocket 客户端
├── mappers/
│   └── sync_data_mapper.dart    # JSON 序列化/反序列化
├── models/
│   ├── models.dart              # 模型导出
│   ├── device_info.dart         # 设备信息
│   ├── sync_log.dart            # 同步日志 (Isar Collection)
│   └── sync_response.dart       # 同步响应
└── repository/
    └── sync_log_repository.dart # 同步日志仓库
```

---

## 核心组件

### 1. SyncService (`sync_service.dart`)

状态管理层，使用 Riverpod 的 `StateNotifier` 模式。

```dart
class SyncServiceState {
  final bool isServerRunning;      // 服务器是否运行
  final bool isSyncing;            // 是否正在同步
  final DeviceInfo? localDevice;   // 本机设备信息
  final List<DeviceInfo> discoveredDevices;  // 发现的设备
  final String? lastError;         // 最后错误
  final DateTime? lastSyncTime;    // 最后同步时间
}
```

主要功能：
- `startServer()` / `stopServer()` - 启停 WebSocket 服务
- `discoverDevices()` - 扫描局域网设备
- `syncWithDevice(ip)` - 与指定设备同步
- `_startLocalDataWatchers()` - 监听数据库变化
- `_onLocalDataChanged()` - 广播变更通知

### 2. SyncWebSocketServer (`realtime/sync_websocket_server.dart`)

WebSocket 服务器，提供设备连接和数据同步。

**端口**：`54322`

**消息类型**：

```dart
class SyncMessageType {
  static const String hello = 'hello';           // 握手
  static const String deviceInfo = 'device_info'; // 设备信息
  static const String dataChanged = 'data_changed'; // 数据变化
  static const String syncRequest = 'sync_request'; // 请求同步
  static const String syncResponse = 'sync_response'; // 同步响应
  static const String ping = 'ping';
  static const String pong = 'pong';
  static const String serverShutdown = 'server_shutdown'; // 服务关闭
}
```

### 3. SyncWebSocketClient (`realtime/sync_websocket_client.dart`)

WebSocket 客户端，连接远程设备。

主要功能：
- `connect(ip)` - 连接远程设备
- `requestSyncAndWait(since)` - 请求同步数据并等待响应
- `notifyDataChanged()` - 通知数据变化
- 自动重连机制

### 4. SyncManager (`sync_manager.dart`)

核心调度器，通过 WebSocket 协调设备间的数据同步。

主要方法：
- `synchronize(ip)` - 与指定设备同步
- `synchronizeViaClient(client)` - 通过已建立的连接同步
- `scanNetwork(subnet)` - 扫描网络设备

**冲突解决策略**：Last-Write-Wins (LWW)
- 使用 `uuid` 作为跨设备唯一标识
- 比较 `updatedAt` 时间戳决定胜者

### 5. SyncDataMapper (`mappers/sync_data_mapper.dart`)

集中管理 JSON 序列化/反序列化。

```dart
class SyncDataMapper {
  // Note 转换
  static Map<String, dynamic> noteToJson(Note note);
  static Note noteFromJson(Map<String, dynamic> json);
  
  // Category 转换
  static Map<String, dynamic> categoryToJson(Category category);
  static Category categoryFromJson(Map<String, dynamic> json);
  
  // 混合变更
  static List<Map<String, dynamic>> combineChanges({
    required List<Note> notes,
    required List<Category> categories,
  });
}
```

---

## 数据模型扩展

为支持同步功能，Isar 模型添加了以下字段：

### Note
```dart
@Index(unique: true)
String? uuid;        // 跨设备唯一标识

int updatedAt = 0;   // 更新时间戳（毫秒）

bool isDeleted = false;  // 软删除标记
```

### Category
```dart
@Index(unique: true)
String? uuid;

int updatedAt = 0;

bool isDeleted = false;
```

### SyncLog (新增)
```dart
@collection
class SyncLog {
  Id id = Isar.autoIncrement;
  
  @Index(unique: true)
  late String remoteIp;        // 远程设备 IP
  
  String? remoteDeviceId;      // 远程设备 ID
  String? remoteDeviceName;    // 远程设备名称
  int lastSyncTimestamp = 0;   // 上次同步时间戳
  DateTime? lastSyncTime;      // 上次同步时间
  int syncStatus = 0;          // 同步状态
  String? lastError;           // 错误信息
}
```

---

## 同步流程

### 1. 初始化流程

```
App 启动
    │
    ▼
初始化 SyncServiceNotifier
    │
    ▼
获取本机设备信息 (DeviceInfo)
    │
    ▼
检查 syncAutoStart 设置
    │
    ├── true  ───▶ 自动启动 WebSocket 服务器
    │
    └── false ───▶ 等待用户手动启动
```

### 2. 设备发现流程

```
用户点击 "发现设备"
    │
    ▼
获取本机 IP 地址
    │
    ▼
提取子网前缀 (如 192.168.1)
    │
    ▼
并发尝试 WebSocket 连接 1-254 地址
    │
    ▼
收集成功连接的设备信息
    │
    ▼
过滤自己，更新 discoveredDevices 状态
    │
    ▼
自动与发现的设备保持 WebSocket 连接
```

### 3. 数据同步流程

```
触发同步（手动 / 实时通知）
    │
    ▼
通过 WebSocket 发送 syncRequest
    │
    ▼
等待 syncResponse（包含变更数据）
    │
    ▼
遍历每条变更：
    │
    ├── 本地不存在 ───▶ 插入新记录
    │
    ├── 远程 updatedAt > 本地 ───▶ 覆盖本地
    │
    └── 本地 updatedAt >= 远程 ───▶ 忽略
    │
    ▼
更新 SyncLog 时间戳
```

### 4. 实时通知流程

```
本地数据变更
    │
    ▼
Isar watchLazy() 触发
    │
    ▼
通过所有 WebSocket 连接发送 dataChanged
    │
    ▼
远程设备收到通知
    │
    ▼
触发增量同步
```

---

## 配置项

在 `AppConfig` 中：

```dart
bool get syncAutoStart => _prefs.getBool('sync_auto_start') ?? false;

set syncAutoStart(bool value) {
  _prefs.setBool('sync_auto_start', value);
}
```

---

## 依赖

```yaml
dependencies:
  uuid: ^4.0.0
  network_info_plus: ^7.0.0
```

---

## 使用示例

### 启动同步服务

```dart
final syncService = ref.read(syncServiceProvider.notifier);
await syncService.startServer();
```

### 发现设备

```dart
await syncService.discoverDevices();
final devices = ref.read(syncServiceProvider).discoveredDevices;
```

### 手动同步

```dart
await syncService.syncWithDevice('192.168.1.100');
```

---

## 安全说明

⚠️ **注意**：当前实现仅适用于受信任的局域网环境。

- 无身份验证机制
- 无数据加密传输
- 建议仅在家庭/办公室内网使用

---

## 未来改进

- [ ] 添加设备配对/授权机制
- [ ] 支持 TLS 加密传输 (wss://)
- [ ] 实现冲突合并（替代 LWW）
- [ ] 添加同步历史记录
- [ ] 支持选择性同步（按分类）
- [ ] 离线队列支持

---

## 更新日志

### v2.0.0 (2025-11-28)
- 🚀 **重大重构**：移除 HTTP，统一使用 WebSocket
- 📉 **简化架构**：单一端口 (54322)，减少复杂度
- 🗑️ **移除文件**：删除 `sync_server.dart`, `sync_client.dart`
- ✨ **新增**：WebSocket 客户端支持 `requestSyncAndWait()` 异步等待

### v1.1.0 (2025-11-28)
- 🔧 **代码优化**：集中 JSON 转换到 `SyncDataMapper`
- 🗑️ **清理**：移除未使用的 `syncable_entity.dart`
- 🐛 **修复**：服务器断开时 UI 状态正确更新
- ✨ **新增**：自动启动开关 (`syncAutoStart`)

### v1.0.0 (2025-11-28)
- 🎉 **初始版本**
- HTTP 同步服务器 (Shelf) + WebSocket 实时通知
- LWW 冲突解决
- 局域网设备扫描
